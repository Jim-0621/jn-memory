# --- 基础配置 ---
# ⚠️ 这里必须填对，否则样式会乱。
baseURL: "https://jim-0621.github.io/jn-memory/"
languageCode: "zh-cn"
title: "J & N 的专属回忆" # 默认标题，可随意改
description: "记录我们相识以来的点点滴滴"
theme: "PaperMod"

# 开启 CJK (中日韩) 语言支持
# 这会让 Hugo 统计汉字的数量，而不是按空格切分
hasCJKLanguage: true

pagination:
  pagerSize: 10 # 首页默认展示文章数量

enableRobotsTXT: true
enableEmoji: true
timeZone: Asia/Shanghai

# --- 菜单栏配置 ---
menu:
  main:
    - identifier: timeline
      name: "🕰️ 时光机"
      url: /archives/
      weight: 1

    - identifier: story
      name: "💑 我们的故事"
      url: /categories/我们的故事/
      weight: 2

    - identifier: travel
      name: "✈️ 足迹"
      url: /categories/足迹/
      weight: 3

    - identifier: tags
      name: "🏷️ 标签"
      url: /tags/
      weight: 4
    
    - identifier: search
      name: "🔍 搜索"
      url: /search/
      weight: 5

# --- PaperMod 主题参数 ---
params:
  env: production
  description: "记录我们相识以来的点点滴滴"
  author: Jim
  defaultTheme: auto # 跟随系统切换深色/浅色模式

  profileMode:
    enabled: true
    title: "CXJ ❤️ YDN" 
    subtitle: "从 2022 年 4 月 9 日开始的故事<br/>今天是第 <span id='days'>0</span> 天"
    imageUrl: "images/home_us.jpg"
    imageTitle: "Love"
    buttons:
      - name: "💑 我们的故事"
        url: "categories/我们的故事/"
      - name: "✈️ 足迹"
        url: "categories/足迹/"

  # 社交链接 (可按需修改或删除)
  socialIcons:
    - name: github
      url: "https://github.com/Jim-0621"
    - name: email
      url: "mailto:1031352726@qq.com"

  # 界面设置
  ShowReadingTime: true     # 显示阅读时长
  ShowShareButtons: true    # 显示分享按钮
  ShowPostNavLinks: true    # 显示上一篇/下一篇
  ShowBreadCrumbs: true     # 显示面包屑导航
  ShowCodeCopyButtons: true # 显示代码块复制按钮
  
  # 侧边栏/底部版权
  footer:
    since: 2025 # 或者是当前年份

  # 开启目录
  ShowToc: true
  
  # 默认不展开目录
  TocOpen: false

outputs:
  home:
    - HTML
    - RSS
    - JSON # 👈 必须加这个，否则搜索功能找不到数据源

# goldmark 允许 HTML
markup:
  goldmark:
    renderer:
      unsafe: true # 解析HTML，支持换行，显示天数